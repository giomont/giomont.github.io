export interface Product {
    id?: string; // Optional field for the product ID
    name: string; // Name of the product
    price: number; // Price of the product
    quantity: number; // Quantity of the product in stock
}